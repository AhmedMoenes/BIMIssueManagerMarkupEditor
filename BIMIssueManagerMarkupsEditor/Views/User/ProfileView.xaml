<UserControl x:Class="BIMIssueManagerMarkupsEditor.Views.User.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BIMIssueManagerMarkupsEditor.Views.User"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="0,0,0,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">

            <hc:Card Grid.Column="0" Margin="10" Padding="10">

                <hc:Card.Header>
                    <StackPanel Margin="8">
                        <TextBlock Style="{StaticResource TextBlockTitleBold}" Text="{Binding CurrentUser.FullName}" />
                        <TextBlock Style="{StaticResource TextBlockSubTitle}" Text="{Binding CurrentUser.Email}" />
                    </StackPanel>
                </hc:Card.Header>

                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                    <Image Source="{Binding UserIcon}" Height="190" Margin="8"/>
                </StackPanel>

                <hc:Card.Footer>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Style="{StaticResource TextBlockLargeBold}" Text="{Binding CurrentUser.CompanyName}"/>
                            <TextBlock Style="{StaticResource TextBlockDefaultBold}" Text="{Binding CurrentUser.Role}" />
                        </StackPanel>

                        <hc:Divider Grid.Column="1" LineStrokeThickness="4" Orientation="Vertical"/>

                        <StackPanel Grid.Column="2">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Label Content="Issues Created" Style="{DynamicResource LabelInfo.Small}" Margin="10"/>
                                <TextBlock Style="{StaticResource TextBlockLargeBold}" Text="{Binding CurrentUser.IssuesCreatedCount}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center">
                                <Label Content="Issues Assigned" Style="{DynamicResource LabelInfo.Small}" Margin="10"/>
                                <TextBlock Style="{StaticResource TextBlockLargeBold}" Text="{Binding CurrentUser.IssuesAssignedCount}"  />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center">
                                <Label Content="Projects Included" Style="{DynamicResource LabelInfo.Small}" Margin="10"/>
                                <TextBlock Style="{StaticResource TextBlockLargeBold}" Text="{Binding CurrentUser.ProjectsIncludedCount}"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </hc:Card.Footer>

            </hc:Card>

        </Grid>
    </Grid>
</UserControl>
